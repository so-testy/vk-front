import React, { useEffect } from 'react';

const Exercise = ({ exercise }) => {
    const [exerciseStructure, setExerciseStructure] = useState(null);

    useEffect(() => {
        setExerciseStructure([])
    }, []);

    return (

    );
}

export default Exercise;